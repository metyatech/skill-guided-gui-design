{
  "name": "skill-guided-gui-design",
  "version": "0.3.0",
  "private": true,
  "description": "Agent skill for designing guided GUIs from HCI and usability principles",
  "scripts": {
    "lint": "markdownlint **/*.md --ignore node_modules --ignore AGENTS.md",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "verify": "npm run format:check && npm run lint",
    "prepare": "husky"
  },
  "devDependencies": {
    "compose-agentsmd": "^3.5.1",
    "husky": "^9.1.7",
    "markdownlint-cli": "^0.48.0",
    "prettier": "^3.8.1"
  }
}
